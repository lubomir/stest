# TODO

* create OutputQueue instead of a pipe (with (almost) unlimited capacity)
* correctly handle crash of the tested program
* add command line argument parsing for stest
* add command line arguments for tested program
* test program using valgrind
* create a tool to simplify creating tests
* test program via custom command (*.cmd)
* enable execution of a script before and after test (*.pre, *.post)

* allow building without tests
